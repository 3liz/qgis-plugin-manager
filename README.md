Mainly designed for QGIS Server plugins.

**WIP**

Not tested on Windows.

```bash
list
remote
update
cache PLUGIN_NAME
install PLUGIN_NAME
install PLUGIN_NAME==VERSION
# info PLUGIN_NAME
```

Todo : exit code, API
